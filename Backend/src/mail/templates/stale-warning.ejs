<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Project Status Alert</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: #f7f9fa;
            color: #222;
            margin: 0;
            padding: 0;
        }
        .container {
            background: #fff;
            max-width: 480px;
            margin: 40px auto;
            padding: 32px 24px;
            border-radius: 8px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.07);
        }
        h2 {
            color: #dc2626;
            margin-top: 0;
        }
        .warning {
            background: #fee2e2;
            border-left: 4px solid #dc2626;
            padding: 16px;
            margin: 16px 0;
            border-radius: 4px;
        }
        p {
            margin: 16px 0;
            line-height: 1.6;
        }
        .details {
            background: #f3f4f6;
            padding: 16px;
            border-radius: 4px;
            margin-top: 24px;
        }
        .details p {
            margin: 8px 0;
        }
    </style>
</head>
<body>
<div class="container">
    <h2>Project Status Alert</h2>
    <div class="warning">
        <p>Your project has been inactive for an extended period.</p>
    </div>
    <p><%= message %></p>
    <div class="details">
        <p><strong>Project:</strong> <%= projectName %></p>
        <p><strong>Last Updated:</strong> <%= new Date(updatedAt).toLocaleDateString() %></p>
        <p><strong>End Date:</strong> <%= new Date(endDate).toLocaleDateString() %></p>
    </div>
    <p>Please update your project status or contact your supervisor if you need assistance.</p>
</div>
</body>
</html>